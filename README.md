# 문자열 덧셈 계산기

## 기능 목록

### 1. 덧셈할 문자열을 입력 받는다.

### 2. 커스텀 구분자를 추출하여, 구분자 리스트를 만든다.

- 문자열 가장 앞부분의 "//"와 "\n" 사이에 위치하는 문자를 추출한다.

### 3. 구분자들을 기준으로 분리한 각 숫자의 합을 구하고, 결과를 출력한다.

- 숫자로 변환되지 않는다면, "[ERROR] 잘못된 형식을 입력하였습니다."라는 에러 메시지를 보여 준다.
- 양수가 아니라면, "[ERROR] 각 숫자는 0 이상이어야 합니다."라는 에러 메시지를 보여 준다.
